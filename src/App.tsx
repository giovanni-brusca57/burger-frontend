/**
 * App.tsx is intentionally minimal — routing and layout are handled by
 * `src/router/index.tsx` and `src/components/layout/AppLayout.tsx`.
 *
 * This file is kept for reference; main.tsx boots the RouterProvider directly.
 */
export default function App() {
  return null;
}
